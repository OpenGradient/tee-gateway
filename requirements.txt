# === OpenAPI / Flask middleware ===
connexion[swagger-ui] < 3.0.0
werkzeug >= 2.2.3
swagger-ui-bundle >= 0.0.2
python_dateutil >= 2.6.0
setuptools >= 21.0.0
Flask >= 3.0.0
gunicorn >= 23.0.0

# === x402 payment middleware ===
og-test-x402 == 0.0.9

# === TEE server / LLM routing ===
fastapi >= 0.128.0
uvicorn[standard] >= 0.40.0
pydantic >= 2.12.5
requests >= 2.32.5
urllib3 >= 2.0.0

# LangChain
langchain >= 0.3.27
langchain-core >= 1.2.7
langchain-openai >= 0.3.35
langchain-anthropic >= 0.3.22
langchain-google-genai >= 4.2.0
langchain-xai >= 0.2.5

# LLM provider SDKs
openai >= 2.15.0
anthropic >= 0.76.0
google-generativeai >= 0.8.6

# Crypto / TEE
cryptography >= 46.0.3

# HTTP
h2 >= 4.3.0
httpx >= 0.28.1
httpcore >= 1.0.9

# System
psutil >= 7.2.1
python-dotenv >= 1.2.1

# # OpenGradient SDK (pinned commit) - use non-editable for Docker
# git+https://github.com/OpenGradient/sdk.git@61f357c5b3a7a2c1d6d6944d5d0be721ffc8069b#egg=opengradient